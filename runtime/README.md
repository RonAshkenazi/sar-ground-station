# Runtime Storage

Local development storage for SAR Ground Station.

```text
runtime/
  DATA/          # scan folders and official scan artifacts
  TEMP/          # non-persistent working artifacts
  Saved Scans/   # persistent save/resume packages
```

Copy demo scan folders into `runtime/DATA/`.

These folders are ignored by git except for `.gitkeep` placeholders.

