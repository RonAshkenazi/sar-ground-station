# Legacy App Reference

Copy the working legacy Ground Station app here when Codex or Claude needs implementation context.

Recommended contents:

- source code
- dependency files
- run instructions
- sample Wi-Fi input data if safe
- expected enriched output artifacts for one successful run
- notes about known bugs or behavior that should not be preserved

Rules:

- Treat this folder as read-only reference context.
- Do not modify the Air Unit / Airborne side.
- Do not commit real datasets, secrets, or private keys.
- Use the Researcher role to compare legacy behavior against `docs/Part A.md`, `docs/Part B.md`, and `docs/Part C.md`.

